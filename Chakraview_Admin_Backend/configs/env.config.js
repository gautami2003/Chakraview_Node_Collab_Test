const dotenv = require('dotenv');
dotenv.config();

const {
  NODE_ENV,
  SMTP_USER,
  PORT,
  DB_URI,
  JWT_SECRET_KEY,
  JWT_EXPIRES_IN,
  JWT_REFRESH_EXPIRES_IN,
  SMTP_EMAIL,
  SMTP_PASSWORD,
  RESET_PASSWORD_URL,
  VERIFICATION_LINK,
  SMTP_PORT,
  SMTP_HOST,
  EMAIL_IS_SECURE,
  EMAIL_SERVICE,
  FRONT_END_URL,
  SMTP_USERNAME,
  DB_HOST,
  DB_USER,
  DB_PORT,
  DB_PASS,
  DATABASE,
  REDIS_HOST,
  REDIS_PORT,
  REDIS_PASSWORD,
  MQTT_USERNAME,
  MQTT_PASSWORD,
  GOOGLE_DISTANCEMATRIX_URL,
  FIREBASE_API_KEY,
  FCM_SEND_MSG_URL,
  MQTT_BROKER_PORT,
  MQTT_BROKER_HOST,
  NOTIFICATION_API_KEY,
  NOTI_API_URL,
  DEBUG,
  TXTGURU_SMS_API,
  TEXTGURU_SMS_USERNAME,
  TEXTGURU_SMS_PASSWORD,
  TEXTGURU_SMS_SOURCE,
  CCA_MERCHANT_ID,
  CCA_ACCESS_CODE,
  CCA_WORKING_KEY,
  CCA_URL,
  MMI_CLIENT_ID,
  MMI_CLIENT_SECRET,
  MMI_AUTH_API,
  MMI_IOT_API,
  BASE_URL,
  AWS_ACCESS_KEY_ID,
  AWS_SECRET_ACCESS_KEY,
  AWS_BUCKET_NAME,
  AWS_REGION,
  AWS_CLOUD_FRONT_URL,
} = process.env;

module.exports = {
  NODE_ENV,
  PORT,
  SMTP_USER,
  DB_URI,
  JWT_SECRET_KEY,
  JWT_EXPIRES_IN,
  JWT_REFRESH_EXPIRES_IN,
  SMTP_EMAIL,
  SMTP_PASSWORD,
  RESET_PASSWORD_URL,
  VERIFICATION_LINK,
  SMTP_HOST,
  SMTP_PORT,
  EMAIL_IS_SECURE,
  EMAIL_SERVICE,
  FRONT_END_URL,
  SMTP_USERNAME,
  DB_HOST,
  DB_USER,
  DB_PORT,
  DB_PASS,
  DATABASE,
  REDIS_HOST,
  REDIS_PORT,
  REDIS_PASSWORD,
  MQTT_USERNAME,
  MQTT_PASSWORD,
  GOOGLE_DISTANCEMATRIX_URL,
  FIREBASE_API_KEY,
  FCM_SEND_MSG_URL,
  MQTT_BROKER_PORT,
  MQTT_BROKER_HOST,
  NOTIFICATION_API_KEY,
  NOTI_API_URL,
  DEBUG,
  TXTGURU_SMS_API,
  TEXTGURU_SMS_USERNAME,
  TEXTGURU_SMS_PASSWORD,
  TEXTGURU_SMS_SOURCE,
  CCA_MERCHANT_ID,
  CCA_ACCESS_CODE,
  CCA_WORKING_KEY,
  CCA_URL,
  MMI_CLIENT_ID,
  MMI_CLIENT_SECRET,
  MMI_AUTH_API,
  MMI_IOT_API,
  BASE_URL,
  AWS_ACCESS_KEY_ID,
  AWS_SECRET_ACCESS_KEY,
  AWS_BUCKET_NAME,
  AWS_REGION,
  AWS_CLOUD_FRONT_URL,
};
